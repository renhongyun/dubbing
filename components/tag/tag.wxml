<view class="wrapper">
<!-- <block wx:if="{{tagList}}"> -->
  <view class="sort" >
    {{sort}}：
  </view>
<!-- </block> -->
  <view class="tags-container">
  <block wx:for="{{tagList}}">
    <view class="tags">
      {{item.name}}
    </view>
  </block>
</view>
</view>